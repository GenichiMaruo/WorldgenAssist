# Worldgen Assist

[English](README.md) | [日本語](README.ja.md)

Worldgen Assist は、プレイヤーの PC がサーバーの新しい地形の計算を手伝う
Fabric MOD です。探索時の地形生成の負担を分担しつつ、ワールドの管理は
サーバーに残します。現在は実験的なアルファ版で、速度向上はまだ実証していません。

対象となる Overworld の新規チャンクが必要になると、MOD は同じプレイヤーの
クライアントに地形の形を決める計算の一部を依頼できます。サーバーは結果を検査し、
通常の vanilla 生成を自ら続行します。鉱石、構造物、流体、ブロック配置、保存、
およびゲームプレイに関わるすべてのワールド状態はサーバー権限のままです。

## アルファ版について

今回の版は **0.1.0-alpha.2+mc26.2** です。この版では、
各プレイヤーが自分の表示範囲内の地形計算を同時に支援できます。
範囲が重なるチャンクは1人に割り当て、既定の全体上限は8ジョブ、1人あたりは
1ジョブです。[複数プレイヤー対応と検証状況](docs/MULTIPLAYER_SUPPORT.md)を
参照してください。246テスト / 52スイートと、2人の同時支援・片方の切断試験を
通過しています。

現段階では条件を限定した試用向けで、公開サーバーでの常用には対応していません。
リモート支援は既定で無効で、速度向上も保証しません。実装済みの
リモート経路は、明示的に信頼した参加者、新規生成の vanilla 互換
Overworld、対象条件を満たす新しい地形に限定されています。敵対的な公開サーバー、
任意のデータパック、他ディメンション、他プレイヤーへの計算共有には対応しません。

[Minecraft 26.2 向け alpha.2 をダウンロード](https://github.com/GenichiMaruo/WorldgenAssist/releases/tag/v0.1.0-alpha.2%2Bmc26.2)
できます（Git タグ `v0.1.0-alpha.2+mc26.2`）。サーバーと参加する各クライアントで
同じ版を使用してください。旧版 alpha.1 は別のリリースとして保持しています。

## 実装されていること

- クライアントとサーバーは別のジェネレーターを作るのではなく、地形密度の計算を
  分担します。
- サーバーは上限付きの入出力検査と、任意で独立したビット完全一致の検証を行って
  から結果を受け入れます。
- タイムアウト、切断、不正な結果、リロード、利用できないワーカーでは、通常の
  サーバー内生成へフォールバックします。
- 旧来の trusted-raw 経路には、可逆 RAW/DEFLATE 圧縮、上限付きのコンテキスト対応
  キャッシュ、プレイヤー所有の予測があります。キャッシュと予測は、下記の公開
  フィクスチャには含まれません。

配布用ビルドは Java 25.0.4 で **246 テスト / 52 スイート**を通過しました。
2つの導入用クライアントプロファイルと別PCのサーバーで、両経路の同時支援を確認し、
公開フィクスチャの588個、trusted-raw経路の382個の共有NOISEダイジェストが
通常生成と一致しました。片方に既存の開発用結果保留を使った切断試験も通過し、
406個が一致しています。検証ログにはタイムアウトやオフライン認証・Realmsエラーが
残っています。
これは記載した範囲での機能確認であり、速度向上、プライベート seed の機密性、
公開サーバーでの安全性を証明するものではありません。証拠は英語の
[テスト結果](docs/TEST_RESULTS_LATEST.md)を参照してください。

## 重要なセキュリティ上の区別

2 つの別個の実験経路があります。

| 経路 | 範囲 | セキュリティ上の意味 |
| --- | --- | --- |
| trusted raw-seed モード | 明示的に信頼したクライアントと、使い捨ての制御されたワールド | サーバーが raw seed を送信します。seed を秘匿すべき場面では使わないでください。 |
| 公開 transcript フィクスチャ | すでに公開されている seed `8675309` のみ | 狭いテスト経路です。プライベート seed の機密性は提供しません。 |

raw seed のフィールドを省くだけでは機密性は保証されません。決定論的 transcript は
候補 seed の照合を可能にすることがあります。プライベートワールドでのリモート生成は、
通常のサーバーで有効にする設定ではなく、研究とセキュリティ上の未解決ゲートです。

## 安全な導入

同じ **MOD JAR** を Fabric クライアントと
Fabric サーバーの両方に入れてください。`-sources.jar` は、どちらの `mods` フォルダー
にも入れないでください。研究上の制約を理解するまでリモート支援は無効のままにし、
必ず使い捨てのワールドで試してください。

| 必要なもの | バージョン |
| --- | --- |
| Minecraft Java Edition | 26.2 |
| Fabric Loader | 0.19.3 |
| Fabric API | 0.156.0+26.2 |
| Java | 25（25.0.4 で確認） |

MOD を動かす場所には Minecraft、Fabric Loader、Fabric API、Java が必要です。
vanilla クライアントが自動的にワーカーになることはありません。実験的な支援経路では、
両側への導入が必要です。

## 詳細情報

- [導入方法（日本語）](docs/INSTALL.ja.md) / [Installation (English)](docs/INSTALL.md)。
- [高度なガイド（英語）](docs/ADVANCED_GUIDE.md) — セットアップ、設定、診断、
  ベンチマーク、研究用スイッチ。
- [アーキテクチャ（英語）](docs/ARCHITECTURE.md) と
  [検証済み worldgen パイプライン（英語）](docs/WORLDGEN_PIPELINE.md)。
- [seed 機密性の説明（英語）](docs/SEED_CONFIDENTIALITY.md) と
  [公開フィクスチャのプロトコル（英語）](docs/SEEDED_LEAF_FIXTURE_PROTOCOL.md)。
- [テストの証拠（英語）](docs/TEST_RESULTS_LATEST.md)。
- [バージョンと Minecraft 別ブランチの管理](docs/VERSIONING.md)。

詳細なプロジェクト文書の多くは現在英語です。この README は、すべての文書の翻訳や
ゲーム内ローカライズを約束するものではありません。

## FAQ

**サーバーは速くなりますか？** 必ずしも速くなりません。性能評価は、制御された
ベンチマークで裏付けられるまで保留されています。

**プライベートなサバイバルワールドで使えますか？** リモート支援には使えません。
trusted raw-seed 経路ではワーカーに seed が渡り、公開フィクスチャは seed `8675309`
専用です。

**クライアントが地形やフィーチャーを決めますか？** クライアントが返すのは途中の
計算結果で、その適用と生成の完了はサーバーが行います。ただし、抜き取り検証は
悪意ある結果をすべて防げる保証ではありません。
