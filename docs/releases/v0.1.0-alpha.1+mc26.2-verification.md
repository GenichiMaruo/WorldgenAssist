# Release verification — 2026-09-11

Version: `0.1.0-alpha.1+mc26.2` (Minecraft 26.2, Fabric, JDK 25.0.4).

## Build and unit tests

Evidence: `test-artifacts/20260911-093806-091/`.
Phase A compile, B focused tests, C full tests and D build all exit 0.
C and D each report **240 tests / 51 suites**, zero failures/errors/skips.
The before/after source and build-configuration manifests have no differences.
All 199 compiled class entries match the preceding 0.1.0 JAR byte-for-byte;
`fabric.mod.json` contains the new version. No Minecraft dependency was upgraded.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `worldgen-assist-0.1.0-alpha.1+mc26.2.jar` | 417210 | `251FC38F2DF85FE3AD20F9E4DB632444E5B9CA822A781DFEBF1F763560C7E50D` |
| `worldgen-assist-0.1.0-alpha.1+mc26.2-sources.jar` | 138676 | `E7AB91E6D88DCF26BBA1EFD3D020F58D056081F6C5144DE25BD03B200763A5FD` |

## Installed-JAR multi-PC runtime

Both the client and server use the versioned distribution JAR above, not dev
class outputs. Separate fresh public-seed worlds use loopback-only Minecraft
behind the authenticated SSH tunnel. No private world was used.

```powershell
.\scripts\Run-MultiPcFixture.ps1 -Mode assisted -ClientRuntime installed
.\scripts\Run-MultiPcFixture.ps1 -Mode vanilla -ClientRuntime installed
.\scripts\Compare-SeededLeafFixture.ps1 -AssistedRoot test-artifacts/multipc-installed-assisted-20260911-094241-512 -VanillaRoot test-artifacts/multipc-installed-vanilla-20260911-094431-208
```

Both runtime summaries report `success=True`. Comparison passes:

- All four applied chunks match: `1005,-2006`, `994,-2006`, `996,-1995`, `997,-2004`.
- All **1,010 shared NOISE digests** match; no source snapshot differences.
- Both clients close naturally with exit 0 and the Minecraft `Stopping!` marker.
- Post-disconnect GC checks report zero targeted live job classes in both runs.
  This is not a deep heap-retention proof or a repeated-session soak test.

The client logs contain expected offline-fixture authentication errors for user
properties/profile certificates (HTTP 401) and Realms authentication/feature
flags. There are empty fish sound warnings and the intentional completion
disconnect; the assisted server also logs a moved-too-quickly warning. The
servers warn about enabled digest logging and offline authentication, restricted
here to loopback through SSH. No additional Mixin, decode or executor errors
were found. These runs do not validate authenticated online-server operation.

The previous test JAR was moved to the remote task's `retired-mods` directory,
not deleted. Final cleanup checks found zero task-owned remote Java processes
and zero fixture-port listeners on either PC. Detailed test logs/worlds remain local test evidence, not release
assets. Earlier adversarial, raw-seed and transport-loss evidence is preserved
in [the consolidated report](../TEST_RESULTS_LATEST.md); it was not rerun as part
of this version-only checkpoint.

## Limits / 制限

Performance is **NOT_EVALUATED**. This alpha does not prove private-seed
confidentiality or hostile-public-server safety. Remote work stays off by
default; the transcript route remains restricted to public seed `8675309`.

配布用の新バージョンで全240テストと複数PCでの実行確認に成功しました。
支援結果を適用した4チャンク、および比較可能な1,010チャンクの NOISE ダイジェストは
すべて通常生成と一致しています。ただし、これは速度向上やシード秘匿性の証明では
ありません。詳細な異常系試験は、上記リンクの以前の検証記録と区別してください。
